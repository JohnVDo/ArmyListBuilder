  <tr>
    <td><%=h squad.troop_choice.name %></td>
    <td><%=h squad.name %></td>
    <td><%=h squad.squad_item.name %></td>
    <td><%=h squad.total_point %></td>
    <td>
      <%= link_to 'Modify', {:action => :modify, :id => squad} %><br />
    </td>
    <td>
      <%= link_to 'Show', squad %>
      <%= link_to 'Edit squad name', edit_squad_path(squad) %>
      <%= link_to 'Destroy', squad, :confirm => 'Are you sure?', :method => :delete %>
    </td>
  </tr>
